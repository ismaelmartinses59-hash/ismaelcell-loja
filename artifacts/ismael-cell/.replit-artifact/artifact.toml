kind = "web"
previewPath = "/"
title = "Ismael Cell"
version = "1.0.0"
id = "artifacts/ismael-cell"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23693

[services.development]
run = "pnpm --filter @workspace/ismael-cell run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ismael-cell", "run", "build" ]
publicDir = "artifacts/ismael-cell/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23693"
BASE_PATH = "/"
